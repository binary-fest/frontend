import { Typography } from '@material-ui/core'
import React, { ReactElement } from 'react'

export default function About(): ReactElement {
  return (
    <div>
      <Typography style={{ color: 'white' }}>This is About</Typography>
    </div>
  )
}
