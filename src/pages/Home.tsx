import { Typography } from '@material-ui/core'
import React, { ReactElement } from 'react'

export default function Home(): ReactElement {
  return (
    <div>
      <Typography style={{color: 'white'}}>This is Homepage</Typography>
    </div>
  )
}
