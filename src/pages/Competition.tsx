import { Typography } from '@material-ui/core'
import React, { ReactElement } from 'react'

export default function Competition(): ReactElement {
  return (
    <div>
      <Typography style={{ color: 'white' }}>This is Competition</Typography>
    </div>
  )
}
